---
type: moc
tags: [moc, src, config]
---

# Config Source MOC

Mirror of `@root/src/config/`. This folder owns typed runtime configuration,
deployment presets, defaults, validation, and environment override precedence.

- [[app-config]] — typed `ACP_*` configuration and live Effect Layer.
- [[app-config.test]] — defaults, profiles, explicit overrides, conversions, and
  unsupported-profile rejection.

## Referenced by

[[src/_MOC]] · [[architecture/_MOC]]
